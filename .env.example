# Public URL of the deployed Corefort AI Worker (no trailing slash).
# Local development: run `npm run dev` inside /worker and use http://localhost:8787
# If this is empty the chat widget is simply not rendered.
NEXT_PUBLIC_COREFORT_AI_URL=http://localhost:8787
